!!!
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %title Jeevan
    = stylesheet_link_tag "application"
    %link{href: "http://fonts.googleapis.com/css?family=Rum+Raisin", rel: "stylesheet", type: "text/css"}/
    = javascript_include_tag "vendor/modernizr"
    = csrf_meta_tags
  %body{:class => yield(:cover)}
    -unless (current_page?(root_path))
      %nav.top-bar{"data-topbar" => ""}
        %ul.title-area
          %li.name
            %h1.logo
              =link_to "Jeevan", (current_user ? submitted_manage_menaces_path : root_path)
        -if current_user.present?
          %section.top-bar-section
            / Right Nav Section
            %ul.right
              %li
                =link_to "Sign out", destroy_user_session_path, :method => :delete

    .row.main-container
      .small-12.columns

        -if notice.present?
          .alert-box{"data-alert" => ""}
            =notice
            %a.close{href: "#"} ×
        -if alert.present?
          .alert-box.warning{"data-alert" => ""}
            =alert
            %a.close{href: "#"} ×
        = yield

    %footer
      Designed and built with all the love in the world during 
      %a{:href => "http://info.thoughtworks.com/CMAHackChennai_RegistrationPage.html", :target => "_blank"} CMU Hackathon 
      by 
      %a{:href => "http://twitter.com/deepakprasanna", :target => "_blank"} @deepakprasanna
      = javascript_include_tag "application"
